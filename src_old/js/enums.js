const channel = {
    chrome:Symbol(),
    aira2:Symbol(),
},status = {
    starting:Symbol(),
    downloading:Symbol(),
    suspend:Symbol(),
    stopped:Symbol(),
    deleted:Symbol(),
    downloaded:Symbol(),
    unknown:Symbol(),
}

export {channel,status}